
class MoveZeroes
{


public void MoveZeroes(int[] nums)
{
int index=0; 

for(int i = 0 ;i <nums.length;i++)
{
if(nums[i]!=0)
{
nums[index]=nums[i];
index++;

}


}

while(index<nums.length)
{
nums[index]=0;
index++;
}

/* Anything that must be remembered across iterations
must be declared outside the loop.
i → changes every iteration → belongs inside for
index → remembers progress → must be outside */
