public class Vowels {
  public static void main(String[] args) {
    String s = "hello JAVA";
    int count = 0;
    String vowels = "aeiouAEIOU";
    
    // Traditional for loop instead of enhanced for loop 
    for (int i = 0; i < s.length(); i++) {
      char c = s.charAt(i);
      if (vowels.indexOf(c) >= 0) { 
        count++;
      }  
    }
    
    System.out.println(count); // 4
  }
}
//for (char c : s.toCharArray()) if (vowels.indexOf(c) >= 0) count++;
/* s = The sentence being analyzed (the "subject")
vowels = The reference list (the "dictionary")

For each character in the sentence, we ask: "Is this character in our vowel dictionary?"
The vowels string doesn't pick characters - it just sits there as a lookup table while each character from s gets checked against it. */
