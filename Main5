class Dog {
    String name;
    int age;

    Dog() {                    // default
        this("Puppy", 0);
    }
    Dog(String name) {         // overloaded 
        this(name, 1);
    }
    Dog(String name, int age) { // main ctor
        this.name = name;
        this.age = age;
    }
} 
