class Box {
    private final int value;   // cannot change later

    Box(int v) {               // only set once, at creation
        value = v;
    }

    int getValue() {           // getter, read-only setter have no return 
        return value;
    }
}
 
